//Pedir 2 números al usuario y sumarlos, restarlos, multiplicarlos y dividirlos. 
let num1= parseInt(prompt ("introduce un numero entero"));
let num2= parseInt(prompt ("introduce un numero entero"));
let sum= parseInt(num1+num2);
alert (`los resultado de la suma es ${sum}`);
let mul= parseInt(num1*num2);
alert (`los resultado de la multiplicacion es ${mul}`);
let rest= parseInt(num1-num2);
alert (`los resultado de la resta es ${rest}`);
let div= parseInt(num1/num2);
alert (`los resultado de la division es ${div}`);
